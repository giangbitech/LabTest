@using BiTech.LabTest.DataObject.ViewModels
@model StudentViewModel.TestDataViewModel
<script>


</script>
<div id="timer" style="display: block; position: absolute;" class="timer-fix">
    <div class="form-clock">
        <form id="clock">
            <span>@LanguageConfig.Dictionary["Student_DoTest_Time"]</span>
            <span id="testTimer"></span>
        </form>
    </div>
</div>

<div class="panel panel-default">
    <div class="panel-body">
        <div class="row">
            <div class="col-md-4 col-md-offset-4 choose-source-body choose-source-container">
                <div>
                    <center>
                        <table class="table table-striped table-bordered">
                            <tr>
                                <td colspan="2">Trường @Model.TestInfo.SchoolName</td>
                                <td colspan="2">@Model.TestInfo.TestType</td>
                            </tr>
                            <tr>
                                <td colspan="2">Năm học @Model.TestInfo.Year</td>
                                <td colspan="2"> @Model.TestInfo.Grade</td>
                            </tr>
                            <tr>
                                <td colspan="2">Môn @Model.TestInfo.Subject</td>
                                <td colspan="2">Thời gian thi @Model.TestInfo.TestTime phút</td>
                            </tr>
                            <tr>
                                <td colspan="2">@Model.StudentName</td>
                                <td colspan="2">Lớp @Model.StudentClass</td>
                            </tr>
                        </table>
                    </center>
                </div>
            </div>
        </div>
        <div class="get-data-caption"><center><h2>@LanguageConfig.Dictionary["Student_Welcome"]</h2></center></div>
        <div id="MessageBox" class="alert-danger panel-danger uploader-message-box"></div>
        <div id="TestArea" class="row">
            @using (Html.BeginForm("DoneTest", "Student", FormMethod.Post, new { id = "DoTestForm" }))
            {
                @Html.HiddenFor(m => m.Base64Code);
                @Html.HiddenFor(m => m.Base64Data);
                @Html.HiddenFor(m => m.StudentIPAdd);
                @*Chọn phần thi - Test Group Choosing*@
                <div class="col-md-6 col-md-offset-3">
                    <table class="table table-striped table-bordered">
                        <tr>
                            @foreach (var testGroup in Model.TestGroupList)
                            {
                                if (testGroup.isForAll)
                                {
                                    <td colspan="2">
                                        <label>@testGroup.Title</label>
                                    </td>
                                }
                            }
                            @for (int i = 1; i <= Model.TestGroupList.Count; i++)
                            {
                                if (!Model.TestGroupList[i-1].isForAll)
                                {
                                    <td colspan="2">

                                        <input type="radio" id="TestGroup-@i.ToString()" name="TestGroupChoose" value="TestGroup-@i.ToString()" />                                        
                                        <!--input type="radio" name="TestGroupChoose" id="opt-testGroup.Title" value="testGroup.Title">
                                            Html.RadioButtonFor(m => m.TestGroupChoose, Model.TestGroupList[i].Title, new { id = "TestGroup"+ i.ToString() })-->
                                        <label for="TestGroup-@i.ToString()">@Model.TestGroupList[i-1].Title</label>
                                    </td>
                                }
                            }
                        </tr>
                    </table>
                </div>

                @*Nội dung thi - Test*@

                <div class="col-md-6 col-md-offset-3">
                    <div class="" id="TestDataPartDiv">
                        <div>

                            <!--Xuất từng phần-->
                            @foreach (var groupQuestionVM in Model.TestGroupList)
                            {
                                <div class="do-test-testGroup-div ">
                                    <div>
                                        <h2>@groupQuestionVM.Title</h2>
                                    </div>
                                    @*Xuất trắc nghiệm - print quiz*@
                                    @foreach (var questionVM in groupQuestionVM.QuestionsList)
                                    {
                                        <div>
                                            @switch ((questionVM as StudentViewModel.QuestionObjectViewModel).QuestionType)
                                            {
                                                case StudentViewModel.QuestionTypeEnum.Quiz:
                                                    Html.RenderPartial("_TracNghiem1LuaChon", (StudentViewModel.SingleQuestion_String_OneChoice_ViewModel)questionVM);
                                                    break;
                                                case StudentViewModel.QuestionTypeEnum.Underline:
                                                    Html.RenderPartial("_TracNghiemGachChan", (StudentViewModel.SingleQuestion_String_OneChoice_ViewModel)questionVM);
                                                    break;
                                                case StudentViewModel.QuestionTypeEnum.QuizN:
                                                    Html.RenderPartial("_TracNghiemNhieuLuaChon", (StudentViewModel.SingleQuestion_Bool_MultiChoices_ViewModel)questionVM);
                                                    break;
                                                case StudentViewModel.QuestionTypeEnum.TrueFalse:
                                                    Html.RenderPartial("_TracNghiemDungSai", (StudentViewModel.SingleQuestion_Bool_MultiChoices_ViewModel)questionVM);
                                                    break;
                                                case StudentViewModel.QuestionTypeEnum.Fill:
                                                    Html.RenderPartial("_TracNghiemDienKhuyet", (StudentViewModel.SingleQuestion_String_MultiChoices_ViewModel)questionVM);
                                                    break;
                                                case StudentViewModel.QuestionTypeEnum.Matching:
                                                    Html.RenderPartial("_TracNghiemNoiCheo", (StudentViewModel.SingleQuestion_String_MultiChoices_ViewModel)questionVM);
                                                    break;
                                                case StudentViewModel.QuestionTypeEnum.Group:
                                                    Html.RenderPartial("_CauHoiChum", (StudentViewModel.GroupQuestionViewModel)questionVM);
                                                    break;

                                            }
                                        </div>

                                    }
                                </div>
                            }
                        </div>
                    </div>
                    <div class="col-md-6 col-md-offset-3">
                        <center>
                            <input id="btnDoneTest" type="submit" value="@LanguageConfig.Dictionary["Student_Done_Test"]"
                                   class="btn btn-primary btn-lg">
                        </center>
                    </div>

                </div>
            }
        </div>
    </div>
</div>
@{Html.RenderPartial("_KeepSession");}
@{Html.RenderPartial("_UnfocusPageEvent");}

@section scripts{
    <script>
        $(document).ready(function () {
            $('#DoTestForm input').change(function () {
                var queryString = $('#DoTestForm').serialize();
                $.ajax({
                    url: "BackupTest",
                    type: "POST",
                    data: queryString,
                    success: function (data, textStatus, jqXHR) {

                    },
                    error: function (jqXHR, textStatus, errorThrown) {

                    }
                });
            });
        });

    </script>
}